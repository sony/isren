'use strict';

const Console = require('./Console');
const CSV = require('./CSV');
const JSON = require('./JSON');
const File = require('./File');

module.exports = {
  Console,
  CSV,
  JSON,
  File
};
